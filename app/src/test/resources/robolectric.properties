# Render unit tests run against the app's actual compileSdk/targetSdk. Without this Robolectric picks
# its own default platform, which can render a different framework from the one the app ships against.
sdk=35
