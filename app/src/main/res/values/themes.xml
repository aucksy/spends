<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!--
      Base application theme. The real visual system lives in Compose (MaterialTheme); this XML
      theme only governs the window chrome and the pre-Compose frame so there is no flash on launch.
      Transparent system bars enable edge-to-edge.
    -->
    <style name="Theme.Spends" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/app_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:windowLayoutInDisplayCutoutMode" tools:targetApi="o_mr1">shortEdges</item>
    </style>

    <!-- Branded splash shown on cold start, then hands off to the main theme. -->
    <style name="Theme.Spends.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_logo</item>
        <item name="postSplashScreenTheme">@style/Theme.Spends</item>
    </style>

    <!--
      Transparent theme for the standalone quick-add overlay (#1): the window is see-through so whatever
      is behind (the home screen) shows through, and ONLY the quick-add bottom sheet is drawn. The sheet
      provides its own scrim/animation, so the window dim is disabled and no window animation is used.
    -->
    <style name="Theme.Spends.Transparent" parent="Theme.Spends">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowAnimationStyle">@null</item>
    </style>

</resources>
