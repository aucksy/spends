<?xml version="1.0" encoding="utf-8"?>
<!-- A 1x1 quick-add widget. Tapping opens the app's quick-add sheet (no data shown, fully private). -->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="40dp"
    android:minHeight="40dp"
    android:targetCellWidth="1"
    android:targetCellHeight="1"
    android:resizeMode="none"
    android:widgetCategory="home_screen"
    android:updatePeriodMillis="0"
    android:description="@string/widget_add_description"
    android:previewImage="@drawable/widget_quick_add_preview"
    android:previewLayout="@layout/widget_quick_add"
    android:initialLayout="@layout/widget_quick_add" />
