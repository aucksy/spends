# Built artifacts
*.apk
*.aab
*.ap_
*.dex
*.class

# Gradle / build
.gradle/
build/
local.properties
captures/
.kotlin/

# Signing — never commit keys
keystore.properties
*.keystore
*.jks

# IDE
.idea/
*.iml
.DS_Store

# ---- Sensitive personal data — NEVER commit (PRD §4.3 / §4.13) ----
# Block by FILE TYPE and a dedicated private/ folder — NOT by name substrings like "Monito"/"SMS",
# which would also (case-insensitively on Windows) swallow source files such as MonitoAdapter.kt.
# Raw SMS exports, account numbers, Monito/Excel history exports, and any derived raw fixtures.
*.xls
*.xlsx
*.csv
*.xml.bak
SMS Backup*.xml
sms-export*.xml
/private/
/raw-data/
fixtures/raw/
